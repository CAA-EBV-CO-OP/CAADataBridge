# RStudio/Positron convenience entrypoint
# This restores the “Run App” button behavior by giving the IDE
# a conventional app.R it can detect. It simply defers to the
# robust launcher (which does preflight checks and diagnostics).

source("launch_mapper.R")

